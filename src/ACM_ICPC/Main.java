package ACM_ICPC;

import java.util.*;

public class Main {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		int team = sc.nextInt();
		int problem = sc.nextInt();
		int log = sc.nextInt();
		if((team >=1 && team<=100) && (problem>=1 && problem<=15) && (log>=0 && log<=10000)) {
			
		}
	}
}
